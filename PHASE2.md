# Photo Publisher — Phase 2

Phase 2 implements the local Publisher Core only: recursive JPEG discovery, SHA-256 content identity, a serializable local state and a deterministic add/update/remove synchronization plan.

## Out of scope

Image resizing, gallery manifest assembly, uploads, GitHub, R2, Vercel, Lightroom, GUI and credentials.

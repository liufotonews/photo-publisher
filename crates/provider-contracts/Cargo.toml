[package]
name = "photo-publisher-provider-contracts"
version = "0.1.0"
edition = "2021"

[dependencies]
sha2 = "0.10"
thiserror = "2"

[target.'cfg(windows)'.dependencies]
windows-sys = { version = "0.61", features = ["Win32_Storage_FileSystem"] }
